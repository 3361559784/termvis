# termvis

Use the `termvis_probe`, `termvis_render_card`, `termvis_render_image`, `termvis_life_frame`, and `termvis_soul_event` MCP tools when terminal-safe visual previews, living terminal state frames, or visual-only companion narration are useful.
Prefer symbolic chafa frames for the CLI persona and use text fallbacks only when the active terminal cannot display color graphics.
